# 03 · 抓包

抓包的作用有两个：**看真实报文长什么样**（比读代码可靠），以及**拿到构造请求所需的 header / 参数**。

---

## 路线 A：PC 端抓（最省事，优先试）

**为什么优先**：Fiddler 会自动装好系统证书，**没有手机那套证书信任问题**；而且 Windows 小程序跟随系统代理，不用配手机。

### 配置

1. Fiddler → `Tools > Options > HTTPS` → 勾 **Decrypt HTTPS traffic**
2. 如果只抓本机，**不需要**开 `Allow remote computers to connect`
3. 打开目标小程序，操作几下，看 session 列表里有没有目标的域名

### ⚠️ PC 端的固有局限

**依赖平台特有 API 的流程，在 PC 上走不完。**

例如 iBeacon 扫描（`wx.startBeaconDiscovery`）在 Windows 版微信里不支持，流程会卡在那一步，**后续请求一个都不会发出来**。

**这决定了你能抓到哪些请求**：

- 能抓到：页面加载、列表查询、状态查询、登录等**不依赖特殊硬件/平台的**请求
- 抓不到：需要扫描、需要定位、需要摄像头/陀螺仪等步骤之后的请求

**应对**：先抓能抓到的，往往已经够用了 —— 见下面「够不够用」一节。

---

## 路线 B：手机端抓（抓 PC 抓不到的那些）

### 配置

1. PC 上 Fiddler → `Tools > Options > Connections` → 勾 **Allow remote computers to connect**，端口默认 8888 → **重启 Fiddler**（必须）
2. 放行防火墙（管理员）：

   ```powershell
   New-NetFirewallRule -DisplayName "Fiddler 8888 LAN" -Direction Inbound `
     -Protocol TCP -LocalPort 8888 -Action Allow -Profile Private
   ```

3. 查本机局域网 IP：`Get-NetIPAddress -AddressFamily IPv4 | Where-Object { $_.IPAddress -notlike '127.*' }`
4. 手机上：
   - 连**同一个局域网**（先确认没开 AP 隔离）
   - 浏览器打开 `http://<PC的IP>:8888` —— **能看到 Fiddler 页面就说明网络通了**，这一步先做
   - 下载 `FiddlerRoot certificate` 并安装（见下）
   - WiFi 设置 → 手动代理 → 填 `<PC的IP>` 和 `8888`

### 证书安装（两条路的差别很大）

**iOS**：

1. 下载后：设置 → 通用 → VPN与设备管理 → 安装描述文件
2. **还要再去**：设置 → 通用 → 关于本机 → **证书信任设置** → 打开对应开关

> 第 2 步是 iOS 抓包第一大坑。只装描述文件不打开开关，会精确地报 `ERR_CERT_AUTHORITY_INVALID`。

**Android**：

1. 设置 → 安全 → 加密与凭据 → 安装证书 → **CA 证书**
2. 装完**杀掉目标 App 重启**（否则它不会重新读证书存储）
3. **Android 7+ 大概率仍然失败**，原因见 [01-环境与平台限制](01-环境与平台限制.md#限制-2android-7-抓微信小程序需要系统证书)

---

## 过滤与导出

### 只留目标域名

右侧 **Filters** 标签：

1. 勾 **Use Filters**
2. **Hosts** 下拉框选 `Show only the following Hosts`
3. 输入框填目标域名（多个用分号）
4. 点 **Actions → Run Filterset now**

⚠️ **Filters 只影响显示，不影响抓取** —— 被过滤掉的请求仍在内存里。

### 导出存档

- 想只导可见的：列表里 `Ctrl+A` → 右键 → `Save > Selected Sessions > in ArchiveZIP`
- 懒得配 Filter 就直接 `File > Save > All Sessions…`，全导出来也行

导出的 `.saz` **就是一个 ZIP**，内部结构：

```
raw/
├── 01_c.txt    客户端请求（原始报文）
├── 01_s.txt    服务端响应
└── 01_m.xml    元数据
```

用 [`tools/parse-saz.ps1`](tools/parse-saz.ps1) 可以直接列出所有 session 并展开指定的那条。

---

## 「够不够用」：抓不到全部也能开工

这是本次实战最重要的经验之一。

**你可能永远抓不到那条"最关键"的请求**（因为它依赖 PC 不支持的硬件 API）。但通常**已经抓到的那几条就够配置了**：

设你想构造 `POST /api/checkin/start`，需要这些参数：

| 参数 | 能否从已抓到的请求里拿到 |
|---|---|
| 后端域名 | ✅ 任意请求 |
| 凭证（token / cookie） | ✅ 任意请求的 header |
| 用户标识 | ✅ 多数请求的 body |
| 业务参数（点位编号、类型 id 等） | ✅ **常在"状态查询"类接口的响应里** |

**关键技巧：找「状态查询 / 详情」类接口。**

这类接口通常返回「你上次干了什么」的完整快照，里面往往就包含了你构造请求所需要的**全套业务参数**，而且是**服务端认可过的组合**。

比从真正那条请求里抄还可靠 —— 因为服务端已经用这些参数成功处理过一次了。

**所以在 PC 上抓不到「开始打卡」那条请求，完全不影响后续构造。** 抓一条「查询上次状态」就够。

---

## 只读优先原则

构造实验时，**先用只读接口验证链路**（如查询类），确认：

- 凭证还有效
- 请求格式正确
- 能拿到正常响应

再去碰有副作用的接口（创建、提交、结束）。这样能把"凭证过期"和"构造错误"两类问题分开。
