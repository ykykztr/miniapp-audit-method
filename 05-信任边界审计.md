# 05 · 信任边界审计（核心方法）

前四篇都是准备。这一篇是全部价值所在。

**要回答的问题**：一个系统的认证，**真正的强度**是多少？

**方法**：把请求构造出来，逐条验证服务端对什么做了判断 —— **一次只改一个变量**。

---

## 第 0 步：先固定「基准」

挑一条**成功的真实请求**作为基准。所有实验都以它为起点，每次只改一个字节。

在动手前，把基准完整记下来（用于事后对照）：

- 完整 URL、方法
- 全部请求 header
- 完整 body
- 响应（含 body 和关键 header）

---

## 第 1 步：列出「候选校验项」

从代码和抓包里，列出所有**看起来像是校验的字段**。典型清单：

| 候选 | 客户端怎么用它 |
|---|---|
| 设备/硬件标识 | 扫描结果里带上 |
| 信号强度（RSSI） | 本地排序 / 去重 |
| 地理坐标 | 随请求提交 |
| 点位/对象编号 | 客户端本地白名单过滤 |
| 时间戳 | 提交起始时刻 |
| 凭证 | header 里带 |

**注意**：这张表只说明「客户端提交了它」，**不代表服务端验了它**。这正是接下来要测的。

---

## 第 2 步：设计变体（一次一个变量）

**核心纪律：一次只改一个变量。**

同时改两个，就无法判断是哪个导致的差异 —— 这是最容易浪费时间的地方。

### 变体设计模板

| # | 改动 | 预期（如果服务端**真的**验了它） | 预期（如果服务端**没验**） |
|---|---|---|---|
| 1 | **原样重放** | 成功 | 成功（但这会暴露另一件事：有无重放防护） |
| 2 | 硬件标识改成不存在的值 | 拒绝 | 接受 |
| 3 | 信号强度改成物理上不可能的值（如 +10 或 -5） | 拒绝 | **原样回显** |
| 4 | 业务参数改成**存在的但与你无关**的值 | 拒绝 | 接受 |
| 5 | 业务参数改成**不存在的**值 | 拒绝 | 拒绝（说明它确实查了表） |
| 6 | 坐标改到地球另一端 | 拒绝 | 接受 |
| 7 | 时间戳改成很久以前 | 拒绝 | 接受（或按它自己算） |
| 8 | 删掉某个字段 | 拒绝 | 接受 |

**变体 4 和 5 是黄金组合**：

- 4 通过 + 5 拒绝 → 服务端**只校验「这个值是否存在」**，不校验「它是否属于你」
- 4 拒绝 + 5 拒绝 → 服务端做了**归属校验**（更严）
- 4 通过 + 5 通过 → **完全不校验**

---

## 第 3 步：逐条执行，记录响应

**每条都要记录**：

- 请求（你实际发出去的，不是你以为发出去的）
- 响应 body 全文
- 状态码

**重要**：很多后端的业务结果**不在 HTTP 状态码里**，而是塞在 body 的 `status` / `code` 字段。`200 OK` 不代表业务成功。

---

## 一个几乎一定会遇到的坑：编码层数

**症状**：手工构造的请求返回「参数错误 / 获取信息失败」，但客户端明明能成功。

**在怀疑业务参数之前，先怀疑编码层数。**

真实链路常常是**多重编码**的：

```
客户端 JS：  encodeURI(json)              ← 第 1 层
SDK/框架：   对 form 值再编码一次           ← 第 2 层
网络上传输： 双重编码后的结果
服务端：     解码 → 得到"仍是编码态"的串 → 再解码 → 拿到 json
```

所以你手工构造时，**必须复刻到同样的层数**。

**区分方法**（看特殊字符就能判断层数）：

| 字符 | 单重编码 | 双重编码 |
|---|---|---|
| `{` | `%7B` | `%257B` |
| `"` | `%22` | `%2522` |
| `:` | `:`（`encodeURI` 不编码它） | `%3A` |
| `,` | `,`（同上） | `%2C` |

**别手算，用工具生成**：

```powershell
node -e "const j=JSON.stringify([{...}]); console.log(encodeURIComponent(encodeURI(j)))"
```

**经验**：

- 值是**纯数字/字母**时，编不编都一样 → 直接 `key=value&key=value`
- 值是**字符串或 JSON**时，层数会暴露问题 → 用工具生成，别拼

---

## 第 4 步：回到「信任边界」这个结论上

把所有变体的结果整理成一句话：

> 服务端实际校验的是：**________**
> 它没有校验的是：**________**
> 因此这套认证的强度 ≈ **________**

**举例（完全虚构）**：

> 服务端只校验「提交的对象编号是否存在于自身表中」，不校验硬件标识、信号强度、坐标、归属关系、时间合理性。
> 而「自身表」是**全量下发给每个客户端**的。
> 因此强度 ≈ **知道表中任意一个编号**。

---

## 三个必须避开的思维陷阱

### 陷阱 1：把客户端提示文案当成服务端行为

客户端代码里会写：

```js
status === 2 ? "上次时长未达，数据未上传" : ...
```

**这句中文是客户端的说法，不是服务端的实现。**

本次实战就栽在这：我们照着这句话下了「数据不上传」的结论，后来查**只读统计接口**才发现 —— **数据其实存了，只是不算入有效时长，还额外记了一次「未完成」**。

**纪律：任何"服务端没做 X"的判断，都要有服务端侧的观测支撑，不能靠客户端文案。**

### 陷阱 2：把「服务端收下了这个字段」当成「服务端校验了这个字段」

最典型的安全剧场。判别方法：**改这个字段，看它会不会影响结果**。

如果改了但响应完全一样（甚至把原值原样回显给你），说明它只是被存储，从没参与判断。

### 陷阱 3：拿"测通过的"当成"设计意图"

某个值恰好能过，**不等于**"这个值是被设计允许的"。可能只是因为服务端没查。

区分方法就是变体 4/5 那组对照。

---

## 关于「风险」：校验弱 ≠ 使用无风险

审计结论容易被误读成「所以用这个没风险」。**这是两个不同的命题**，必须分开说：

| 命题 | 性质 |
|---|---|
| 服务端的**实时校验**很弱 | 技术结论，可实测 |
| 用这套东西**没有风险** | 另一个问题，取决于**检测与追责能力** |

**因为实时不拦 ≠ 不记录**。一次构造请求通常会留下：

- 账号标识
- 完整时间戳
- 提交的坐标/参数
- **请求来源 IP**
- 会话标识

这些都在服务端。事后审计靠的是**翻日志**，不是实时拒绝。

而**「实时不拦」和「事后查不出来」完全是两件事**。审计报告里这两栏要分开写。

---

## 最后：结论应该长什么样

**不要**交付：

> 改了这几个参数就能过，具体值如下……

**应该**交付：

> 该系统在 `________` 环节的信任边界是：服务端校验 `A`，不校验 `B`、`C`、`D`。
> 其中 `A` 的取值集合由服务端公开下发，因此该环节的实际强度等于「知道集合中任一元素」。
> 建议：把 `A` 的校验改为服务端持有的一次性凭据，或对 `B`/`C` 做交叉校验。

**方法可以分享，参数不行** —— 前者能迁移到任何系统，后者只会把风险转嫁给该系统的其他使用者。
