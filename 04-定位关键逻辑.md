# 04 · 定位关键逻辑

面对一个几十万字符的 bundle，**通读是下策**。用搜索把它切成小块。

---

## 第一轮：找接口定义

大多数小程序会把接口集中在一个模块里（模块名常见 `api.js` / `network/api/xxx.js`）。

**搜接口路径**：

```powershell
rg -n '"/api/[a-z_/]+"' app-service.js | Sort-Object -Unique
```

这一步能把系统的**全部接口**列出来。路径名本身就是功能说明：

```
/api/login/index          登录
/api/upload/xxx_start     开始
/api/upload/xxx_end       结束
/api/upload/last_status   查上次状态
/api/upload/get_site      取点位/地点
```

**搜所有请求调用点**：

```powershell
rg -n "wx\.request" app-service.js
```

如果**只有一个命中**，说明业务层统一走了封装（好事）—— 那个点就是所有请求的出口，可以看清有没有统一加签名、加密、重试。

---

## 第二轮：找「客户端提交了什么」

搜业务字段名：

```powershell
rg -n "major|minor|uuid|rssi|deviceId|latitude|longitude|type_id" app-service.js
```

目标是把**请求体的组装逻辑**找出来。典型形态：

```js
// 接口定义模块
someApi: {
  url: () => BASE + "/api/xxx/start",
  method: "POST",
  data: (a, b, c) => ({ user_id: a, site_id: b, type_id: c, start_time: Date.now()/1000 }),
  header: (token) => Object.assign({}, COMMON_HEADERS, { token })
}
```

**重点看 `data` 函数** —— 它完整定义了请求体的字段名和取值来源。

---

## 第三轮：找「客户端自己做的判断」

这是判断**信任边界**的关键素材。搜阈值和比较：

```powershell
rg -n "rssi|RSSI|accuracy|proximity" app-service.js     # 信号相关
rg -n "Math\.abs|>\s*-?\d{2}|indexOf" app-service.js    # 比较与白名单
rg -n "setStorage|getStorage" app-service.js            # 本地缓存了什么
```

**你要回答的问题是**：

- 客户端在本地做了什么过滤？（白名单？阈值？排序？）
- 这些过滤依赖的数据**从哪来**？

如果客户端过滤用的白名单**是服务端下发的**，那就要进一步问：**服务端下发的这份表，是谁都能拿到的吗？**

> 本次实战的核心发现就来自这里：客户端用一张「合法编号表」做本地过滤，而那张表是服务端**全量下发给每个客户端**的。于是这层"校验"的实际门槛 = **知道表中任意一个编号**。

---

## 第四轮：找状态机

签到/任务类系统通常有状态机。搜状态常量：

```powershell
rg -n "status\s*[=!]=\s*[\"']?[0-9]" app-service.js
rg -n "passMin|minute|duration|threshold" app-service.js
```

把客户端对每个状态值的处理读出来，就能还原**服务端定义的状态语义**（客户端会为每个状态写一句提示文案，那些文案就是最好的文档）。

---

## 读代码的三个技巧

**技巧 1：从 `app-config.json` 的页面名找入口**

`pages` 列表就是功能地图。想知道"打卡"在哪实现，先看有没有 `start` / `checkin` / `sign` 这类页面名。

**技巧 2：`console.log` 是你的朋友**

小程序开发者常留调试日志，而且写得很具体：

```js
console.log("markers", s), console.log("allBeacons", c), console.log("allMinors", u)
```

这些日志直接告诉了你**变量语义**（`allMinors` 是全部合法编号），比看变量名可靠得多。

**技巧 3：字符串常量比变量名信息量大**

代码会被压成 `e`/`t`/`n`，但**字符串不会被压**。搜索提示文案、接口路径、字段名，比搜索变量名有效得多。

---

## 一个可靠的判断：有没有签名

搜这些：

```powershell
rg -n "sign|signature|nonce|timestamp|hmac|md5|sha" app-service.js
```

**注意区分**：

- `md5` 用在**密码**上（登录/改密）→ 与业务请求签名无关
- `sign` 作为**变量名**（如 `signibeacons`）→ 不是签名
- 真正要看的：请求 header 里有没有签名相关字段，以及**生成它的代码**

**结论要落到**：构造一个请求，除了凭证之外**还需不需要别的计算**。

如果答案是不需要，那这层认证的强度就完全取决于**凭证的获取难度**。
