# 01 · 环境与平台限制

## 工具清单

| 用途 | 工具 | 说明 |
|---|---|---|
| 抓包 | **Fiddler Classic** | 免费，Windows 原生，能导出 `.saz` 存档 |
| 抓包（备选） | mitmproxy / Charles | 跨平台 |
| 解包 | **wxapkg**（Go 写的那个） | 支持微信 v3/v4 路径探测和加密包解密，有 CLI + GUI 两个版本 |
| 搜索 | **ripgrep (`rg`)** | 解包出来的 bundle 动辄几十万字符，`rg` 比 `grep` 快一个量级 |
| 发请求 | **`curl.exe`** | Windows 10 1803+ 自带（注意不是 PowerShell 里那个 `curl` 别名，必须带 `.exe`） |
| 运行时 | Node.js | 复刻 JS 侧的编码逻辑时很有用 |

---

## 三个平台级硬限制

这三条是**踩出来的**，不知道的话会在错误的方向上白花很多时间。

### 限制 1：PC 微信不支持 iBeacon 系列 API

- 现象：调用 `wx.startBeaconDiscovery` 返回 `fail`，**是 API 层直接失败**，不是"没扫到设备"
- 区分要点：
  - `{"errMsg":"startBeaconDiscovery:fail"}` → **API 不支持**
  - 业务层返回空列表 → **API 支持，只是周围没设备**
- 影响：**任何依赖 iBeacon 扫描的流程，在 PC 上都走不完**。流程会卡在扫描那一步，后续请求一个都不会发出
- 官方对 iBeacon 系列 API 的支持范围是 **iOS / Android**

> **推论**：如果你在 PC 微信上操作到某个步骤就断了，先确认那一步是不是调用了平台不支持的 API —— 而不是怀疑自己操作错了。

### 限制 2：Android 7+ 抓微信小程序需要「系统证书」

- 现象：手机上浏览器能抓到 HTTPS 明文，但小程序里请求报 `ERR_CERT_AUTHORITY_INVALID`
- 原因：Android 7.0 起，`targetSdkVersion >= 24` 的应用**默认不信任用户安装的 CA**，只认系统区证书。小程序用的 WebView 内核属于这类
- 无 root 基本绕不过。可选路径：
  - **换 iOS 设备抓**（iOS 装完证书后还要手动打开「证书信任设置」开关，这是第一大坑）
  - **Android root**：把证书塞进 `/system/etc/security/cacerts/`（改名为 `<hash>.0`），或用 Magisk 模块 `MagiskTrustUserCerts`
  - 部分厂商机（如小米）解锁 BL 成本很高（答题 + 绑定等待期 + 清空数据），**先评估值不值**
- **无成本的办法**：走 PC 端抓包（见 [03-抓包](03-抓包.md)）

### 限制 3：微信 v4 的小程序包路径变了

- v3：`%USERPROFILE%\Documents\WeChat Files\Applet\<appid>\<version>\`
- **v4**：`%APPDATA%\Tencent\xwechat\radium\users\<uid>\applet\packages\<appid>\<version>\`
- 影响：解包工具的「扫描本机小程序」功能如果只认 v3 路径，在 v4 上会报"路径不存在"，需要手动指定目录

---

## 网络相关

- GitHub 的 `release` 直链在某些网络下不通，但 `api.github.com` 往往正常
  - **取资产清单用 API**，**取文件用镜像**（`ghproxy.net` / `gh-proxy.com` 这类，可用性会变，自行测）
- 验证下载完整性：优先用官方 `checksums` 文件；注意有些项目给的是 **MD5** 而不是 SHA256
- 检查可下载性时用 `curl -r 0-1023`（取前 1KB），比 HEAD 可靠 —— 有些 CDN 不支持 HEAD，有些返回 403 但 GET 正常

---

## 一个容易被忽略的干扰源

**代理软件（Clash / V2Ray 等）的 TUN 模式会接管所有出站流量。**

抓包期间开着它，可能出现：请求从你的机器出去、但目标服务器看到的 IP 是代理节点；或者连接直接失败。

- 抓包前**先关掉 TUN 模式**
- 判断方法：查有没有 `Mihomo` / `Clash` 之类的虚拟网卡处于 Up 状态
- 系统代理（`ProxyEnable`）和 TUN 是两回事，前者不冲突、后者要关
