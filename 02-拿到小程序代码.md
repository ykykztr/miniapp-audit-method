# 02 · 拿到小程序代码

目标是拿到可读的 JS，用来**读出「客户端声称它提交了什么」**。

## 第 1 步：定位包文件

关键前提：**先在客户端打开一次目标小程序**，它才会落盘。

### 微信 v3

```
%USERPROFILE%\Documents\WeChat Files\Applet\<appid>\<version>\
```

### 微信 v4（现在的主流）

```
%APPDATA%\Tencent\xwechat\radium\users\<uid>\applet\packages\<appid>\<version>\
```

`<uid>` 是一串 32 位十六进制；`<version>` 是数字。

**一次搜全**：

```powershell
Get-ChildItem -Path "$env:USERPROFILE\Documents", "$env:USERPROFILE\AppData" `
  -Recurse -Filter *.wxapkg -ErrorAction SilentlyContinue |
  Sort-Object LastWriteTime -Descending |
  Select-Object -First 30 LastWriteTime,
    @{n='MB';e={[math]::Round($_.Length/1MB,2)}}, FullName
```

包名规则：

- `__APP__.wxapkg` —— 主包
- `_subpackage_xxx_.wxapkg` / `_pages_xxx_.wxapkg` —— 分包
- `__PLUGINCODE__.wxapkg` —— 插件代码（第三方组件，通常不是你要找的业务逻辑）

### Android 端（需要 root）

```
/data/data/com.tencent.mm/MicroMsg/<32位hash>/appbrand/pkg/
```

## 第 2 步：解包

以 `wxapkg`（Go 版）为例：

```powershell
# 单个小程序目录（会自动找里面的版本目录）
wxapkg.exe unpack -r "<packages>\<appid>" -o .\out

# 有分包的话，通常同一个 -r 目录能一起处理
```

> 注意：这类工具的「扫描本机已安装小程序」功能可能只认 v3 路径。v4 上要**手动把目录传进去**，别指望它自动找到。

**关于包加密**：微信 PC 端较新版本的小程序包是加密的。选工具时确认它支持解密 —— 有些老工具需要先跑一遍解密脚本。

## 第 3 步：认识产物

解出来通常是这样：

```
out/
├── <version>/
│   ├── app-service.js        ← 业务逻辑全在这里，主要看它
│   ├── app-config.json       ← 页面路由、窗口配置、权限声明
│   ├── page-frame.html       ← 页面结构（模板 + 样式，通常是 HTML，不是原始 wxml）
│   └── assets/               ← 图片等静态资源
```

**先读 `app-config.json`**，它体积小、信息密度高：

- `pages` —— 全部页面路径，**页面名就是功能地图**（`signin` / `start` / `record` / `rank`…）
- `page[].window.navigationBarTitleText` —— 每个页面的标题
- `permission.scope.userLocation.desc` —— 权限申请文案，能看出它要定位干什么
- `entryPagePath` —— 入口页

**再看 `app-service.js`**。它可能几十万字符，**不要通读** —— 用搜索定位，见 [04-定位关键逻辑](04-定位关键逻辑.md)。

## 关于反混淆

解包出来的代码通常：

- **没有混淆**（大部分中小体量小程序）—— 变量名是 `e` / `t` / `n` 这种压缩后的单字母，但结构完整、字符串明文
- 被 `require` / `define` 切成模块，模块名保留（如 `network/api/api.js`），**这是最有用的线索**

**模块名本身就是地图**。看到 `network/api/xxx.js` 就知道接口定义在里面；看到 `utils/md5.js` 就知道哪里用了 md5。

## 一个实用判断

如果 `app-service.js` 里能直接搜到 `wx.request`、接口路径字符串、业务字段名 —— 说明**没有强混淆**，静态分析完全可行。

如果搜不到（全是短字符串拼装、有 `eval`、有 base64 大块）—— 说明做了处理，静态分析的性价比下降，**改为以抓包为主**。
