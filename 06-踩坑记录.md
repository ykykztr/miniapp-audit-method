# 06 · 踩坑记录

按"浪费时间的程度"排序。

---

## 1. 编码层数 —— 最大的时间黑洞

**症状**：手工构造的请求返回「参数错误 / 获取信息失败」，但真实客户端明明能成功。

**过程**：我们先怀疑业务参数（换了好几个取值），又怀疑是不是有什么隐藏校验 —— 白白试了两轮。

**真相**：请求体需要**双重 URL 编码**，我们只做了一层。详见 [05-信任边界审计](05-信任边界审计.md#一个几乎一定会遇到的坑编码层数)。

**教训**：

- **构造请求失败时，无论看起来多像业务问题，先复现一遍客户端的确切编码方式。**
- 判断层数看特殊字符：`%7B` 是一层，`%257B` 是两层。
- **别手算**，用 `node -e` 生成。

---

## 2. 把客户端提示文案当成服务端行为

**症状**：代码里写着「XX 数据未上传」，于是得出「服务端没有这次记录」的结论。

**真相**：查只读统计接口后发现 —— **数据存了**，只是不计入有效量，而且还额外记了一次「未完成」。

**教训**：**客户端文案是给用户看的，不是给审计看的。** 任何"服务端没做 X"的判断，必须有服务端侧观测。

---

## 3. 把"API 不支持"误判成"没找到设备"

**症状**：某个扫描类操作在 PC 上没结果，第一反应是"周围没有设备"。

**真相**：PC 版微信**根本没有实现**那个 API，调用直接 `fail`。

**区分方法**：

- `{"errMsg":"startBeaconDiscovery:fail"}` → **API 层失败**
- 业务层返回空列表 → API 正常，只是没结果

**教训**：报错信息要看**是谁报的** —— 是平台 API 的 `fail`，还是业务代码的提示。

---

## 4. 在错误的平台上试图复现全流程

**症状**：想抓某个「开始」请求，但在 PC 上怎么操作都抓不到。

**真相**：那条请求在流程的后半段，而流程在中间就撞上了 PC 不支持的 API，**永远走不到那一步**。

**教训**：

- 先确认**流程的哪一步用了平台特有 API**，那一步之后的所有请求都抓不到
- 但**通常不需要** —— 见 [03-抓包](03-抓包.md#够不够用抓不到全部也能开工)：抓一条「状态查询」往往就能拿到全部所需参数

---

## 5. 空跑模式返回的"成功"是假的

**症状**：用脚本的"只打印不发送"模式验证配置，看到 `status=1`，以为链路通了。

**真相**：那个成功响应是**脚本自己编的占位符**，根本没发请求。

**教训**：验证连通性必须用**真实请求**，最好挑只读接口。别把 dry-run 的假响应当成证据。

---

## 6. 改配置改到了模板文件

**症状**：明明改了配置，脚本还报「字段未填写」。

**真相**：改的是 `xxx.config.example.json`（模板），而脚本读的是 `xxx.config.json`。

**教训**：

- 模板文件建议**用不同的扩展名或放到子目录**，别只靠文件名区分
- 报错信息里**一定要打印实际读取的路径** —— 这样用户一眼能看出改错了文件

---

## 7. 状态残留：实验会留下痕迹

**症状**：做实验时用很短的时长"安全收尾"，以为不留痕迹。

**真相**：服务端确实不计入有效量，但**额外记了一次「未完成」**，而且这个计数**用户自己就能看到**。

**教训**：

- 动手前先想清楚：**这次操作会在服务端留下什么状态？**
- 想验证"不留痕迹"这件事本身，需要**只读接口**来确认，而不是靠推断
- 如果目标是"不产生副作用"，**优先找只读接口**做实验

---

## 8. 网络与工具的小坑

| 坑 | 现象 | 处理 |
|---|---|---|
| GitHub release 直链不通 | `curl` 超时，但 `api.github.com` 秒回 | 取清单用 API，取文件用镜像 |
| 代理软件 TUN 模式 | 抓包结果异常 / 连接失败 | 抓包期间关掉 TUN |
| `curl` vs `curl.exe` | PowerShell 里 `curl` 是别名，行为不同 | 显式写 `curl.exe` |
| 表单请求用错参数 | `--data` 会对内容做处理 | 需要原样发送时用 **`--data-raw`** |
| `.bat` 必须 CRLF | 用 LF 存盘时执行异常 | 用工具写出 CRLF，且尽量纯 ASCII |
| 含中文的 `.ps1` | 被按 GBK 读取导致语法错误 | 脚本正文写纯 ASCII，中文放注释外 |
| 检查某个 URL 是否可下载 | `HEAD` 请求返回 403 | 用 `curl -r 0-1023` 取前 1KB |

---

## 9. 一个关于"顺序"的经验

**做实验的顺序应该是：只读 → 幂等写 → 有副作用的写。**

先跑只读接口（查询类）确认链路和凭证，再碰有副作用的。这样出问题时能区分"凭证过期"和"构造错误"——否则两个变量混在一起，又要多花一轮。

---

## 10. 最省时间的一条

**卡住超过 20 分钟，就回到"确切的原始报文"上。**

几乎所有难缠的问题，最后都是靠**看客户端实际发出去的那一个字节流**解决的 —— 而不是靠读代码推断。

抓包、导出、逐字节对照。这是最笨也最快的方法。
